<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>
    <link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/jquery.dataTables.min.css">
     {% block style %}
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: #faefe0;
            transition: background 0.3s ease;
        }
        .topbar {
    width: 100%;
    height: 60px;
    background: #e6c49a; /* Changed this line */
    color: white;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 20px;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
    position: fixed;
    top: 0;
    left: 0;
    z-index: 1000;
}

        .topbar h1 {
    font-size: 22px;
    margin: 0;
    font-weight: bold; /* Makes the title bold */
    color: black; /* Changes the title color to black */
}

        .logout-btn {
            background: red;
            color: white;
            border: none;
            padding: 8px 15px;
            cursor: pointer;
            border-radius: 5px;
            font-size: 14px;
        }
        .sidebar {
            width: 250px;
            height: 100vh;
            background: #e6c49a;
            color: white;
            position: fixed;
            top: 60px;
            left: 0;
            padding: 30px 20px;
            transition: width 0.3s ease;
            overflow: hidden;
            box-shadow: 4px 0px 10px rgba(0, 0, 0, 0.2);
        }
        .sidebar h2 {
            text-align: center;
            font-size: 20px;
            margin-bottom: 20px;
        }
        .sidebar ul {
            list-style: none;
            padding: 0;
        }
        .sidebar ul li {
            padding: 15px;
            text-align: left;
            display: flex;
            align-items: center;
            transition: background 0.3s ease;
            cursor: pointer;
        }
        .sidebar ul li:hover, .sidebar ul li.active {
            background: #efa54f;
        }
        .sidebar ul li a {
            color: white;
            text-decoration: none;
            display: flex;
            align-items: center;
            width: 100%;
        }
        .sidebar ul li i {
            margin-right: 15px;
            font-size: 20px;
        }
        .content {
            margin-left: 270px;
            margin-top: 80px;
            padding: 40px;
            transition: margin-left 0.3s ease;
        }
    </style>
   {% endblock %}
    
</head>
<body>
   <div class="topbar">
    <h1>Admin Dashboard</h1>

    <!-- Logout button as a form to trigger the logout -->
    <form action="{{ path('app_logout') }}" method="post" style="display: inline;">
        <button type="submit" class="logout-btn">Log Out</button>
    </form>
</div>

    <div class="sidebar">
        <h2>Admin Panel</h2>
        <ul>
            <li><a href="{{ path('app_notification_index') }}"><i>🏠</i>Dashboard</a></li>
             <li><a href="{{ path('app_user_index') }}"><i>👥</i>Users</a></li>
            <li><a href="{{ path('app_crud_tuteur') }}"><i>🎓</i>Tuteurs</a></li>
            <li><a href="{{ path('app_reclamation_admin_index') }}"><i>📩</i>Réclamations</a></li>
            <li><a href="{{ path('app_crud_dons') }}"><i>💰</i>Dons</a></li>
            <li><a href="{{ path('app_activite_index') }}"><i>🏃</i>Activities</a></li>
            <li><a href="{{ path('afficher_visite') }}"><i>🏥</i>Visites</a></li>
            <li><a href="#"><i>⚙️</i>Settings</a></li>
        </ul>
    </div>
  <div class="content">
   {% block body %}
   {% endblock %}
</div>

    <script>
        $(document).ready(function() {
            $('#userTable').DataTable();
        });
    </script>
</body>
</html>
